<https://github.com/r-walsh/blueprint>

* `npm i`, `npm start` to develop
* `npm i`, `npm run prod` to start in production
* `npm test` to test
* `npm run` to see additional scripts, including linting

